load("@rules_go//go:def.bzl", "go_binary")

go_binary(
    name = "build_zip",
    srcs = [
        "build_info.go",
        "build_zip.go",
        "manifest.go",
    ],
    visibility = ["//visibility:public"],
)
